package accessModifiers;

public class AccessModifiers {
	
	//Access Modifiers in Java: set of keywords used to control the access of Java elements (data, methods, classes)
	
	//Types of Access Modifiers:
	//1. public : accessible from any other class (data, methods, classes)
	//2. protected : accessible within the same package and subclasses (data, methods)
	//3. default (no modifier) : accessible only within the same package (data, methods, classes)
	//4. private : accessible only within the same class (data, methods)
}
